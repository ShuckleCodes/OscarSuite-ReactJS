# Awards Show Suite Server Environment Variables

# Server port (cPanel will set this automatically)
PORT=8001

# Node environment
NODE_ENV=production

# Optional: Path to public_html if serving React from Node.js
# PUBLIC_HTML_PATH=/path/to/your/public_html

# TMDB API key for movie/person image search
TMDB_API_KEY=your_tmdb_api_key_here
